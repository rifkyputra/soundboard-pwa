// get string and transform to SymbolData[]
// then add a SymbolData from param,
// then convert back to json string
// function appendDataToJson<T>(jsonString: string, newValue: T) {
//     const parsedValue: T[] = JSON.parse(jsonString);
//     const newValueArray = [...parsedValue, newValue];
//     return JSON.stringify(newValueArray);
// }

// function appendListToJson<T>(list: T[], newValue: T) {
//     const newValueArray = [...list, newValue];
//     return JSON.stringify(newValueArray);
// }
